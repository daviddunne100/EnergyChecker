@model IEnumerable<EnergyChecker.Models.usersAppliances>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Appliance.ApplianceTitle)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Tariff)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Appliance.ApplianceTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Tariff)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.usersAppliancesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.usersAppliancesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.usersAppliancesID })
        </td>
    </tr>
}

</table>
